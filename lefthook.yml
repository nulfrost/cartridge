pre-commit:
  parallel: true
  commands:
    lint:
      run: pnpm biome lint
      glob: "*.{ts,tsx}"
      exclude:
        - src/*.ts
      root: "app/"

commit-msg:
  commands:
    commit:
      run: pnpm commitlint --edit {1}
